/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable prefer-const */
const today = new Date();
today.getMonth();

const person = {
  age: 20,
};

class Color {}
let red = new Color();
